String methods
==================================================================
string name = "Rahul Kumar";

1. Split into string array
    name.Split(" "); // ["Rahul", "Kumar"]
    s.Substring(0,3) // will return "Rah"
    s.Replace("a", "Z") // RZhul KumZr
    s.Trim() //. remove leading trailing spaces
    string.Join("O", s.Split(" "));   // return new string 
    s.Contains("o") // false
    s.IndexOf("a") // 4
    s.StartsWith("Ra") // true
    s.EndsWith("ar") // true
    string.IsNullOrEmpty(s) // true
    s.ToUpper() // RAHUL Kumar
    s.ToLower() // rahul Kumar
    s.Length // 11

2. reverse a string
    char[] arr = s.ToCharArray();
    Array.Reverse(arr);
    new String(arr);

    string rev = "";
    int n = s.Length;
    for(int i =n-1; i>=0; i--){
        rev += s[i];
    }
    Console.WriteLine(rev);

3. Array methods:

Array.Sort(arr, (a,b)=>a-b);
Array.Reverse(arr);
Array.IndexOf(10);
Array.BinarySearch(arr, 20)
Array.Find(arr, num=> num == 30)

4. using System.LINQ
    you can use it on collection.

    .Where(x=>x==20)
    .Max()
    .Min()
    .Single(x=>x==20)
    .Any(x=>x==20)  // true
    .FirstOrDefault()
    .Find
    arr.OrderByDescending(num=>num).ToArray() // by default it works for List
    arr.OrderByDescending(num=>num).ToArray() // ascending